# Optionnel : URL de l'API backend.
# En dev avec "npm run dev", ne rien mettre : le proxy Vite envoie /api vers http://localhost:5000.
# En prod ou si le backend est ailleurs, décommenter et adapter :
# VITE_API_URL=http://localhost:5000/api
